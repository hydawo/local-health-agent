"""health-agent — local-first Q&A over your own health data.

Build status: milestone 1 (HealthKit XML -> SQLite + CLI aggregates).
See local-health-agent-plan.md §8 for the milestone order.
"""

__version__ = "0.1.0"
